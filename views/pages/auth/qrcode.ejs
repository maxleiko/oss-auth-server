<!DOCTYPE html>
<html>
  <head>
    <%- include('../../partials/header', { title: title }) %>
  </head>
  <body>
    <%- include('../../partials/top', { active: active }) %>
    <div class="container-fluid">
      <h3>Google Authenticator QR code:</h3>
      <% if (unknown) { %>
        <p class="text-danger">Unknown id <strong><%= id %></strong></p>
      <% } else { %>
        <img class="qrcode" src="<%= qrcode %>" />
        I have my code, let me <a href="/">log in</a>.
      <% } %>
    </div>
  </body>
</html>
